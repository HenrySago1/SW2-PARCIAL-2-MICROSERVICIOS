{
  "query": "query Login($email: String!, $password: String!) { login(email: $email, password: $password) { success message usuario { id nombre email rol activo } token } }",
  "variables": {
    "email": "admin@turisago.com",
    "password": "admin123"
  }
} 